# City Map Visualization

This project visualizes city locations (landmarks and hospitals in Delhi) on an interactive 2D map using Python and the `folium` library.

## Requirements
- Python 3.8+
- Libraries: `pandas`, `folium`

## How to Run
1. Install the required libraries:
   ```bash
   pip install pandas folium